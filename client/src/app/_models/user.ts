export interface User {
    username: string;
    knownAs: string;
    gender: string;
    token: string;
    photoUrl?: string;
    roles: string[];
}


/*

 Key Features:
// Static types (e.g. string, number, boolean)

// Interfaces to define object shapes

// Classes and OOP features

// Enums, Tuples, Union types

// Compiles to JavaScript

*/